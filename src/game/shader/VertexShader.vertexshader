#version 330 core

// Input vertex data, different for all executions of this shader.
layout(location = 0) in vec3 vertexPosition_model_space;

void main(){

    gl_Position.xyz = vertexPosition_model_space;
    gl_Position.w = 1.0;

}
